const config = {
  api: {
    baseUrl: "http://localhost:3005",
  },
  view: {
    results: {
      minLimit: 1,
      maxLimit: 20,
      defaultLimit: 10,
    },
  },
  pagination: { limit: 6 },
}

export default config
